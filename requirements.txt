# ===== BERT Depression Severity (portable requirements) =====
# Tested with Python 3.10–3.11
# This set is GPU/CPU agnostic (PyTorch defaults to CPU wheels).
# If you need CUDA wheels, use requirements-cu121.txt instead.

# Core
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# NLP stack
transformers==4.43.3
tokenizers==0.15.2
datasets==2.20.0
accelerate==0.33.0
evaluate==0.4.2
sentencepiece==0.2.0
huggingface-hub==0.23.4

# Science / metrics
numpy==1.26.4
scipy==1.12.0
scikit-learn==1.4.2
pandas==2.2.2

# Viz & utils
matplotlib==3.8.4
tqdm==4.66.4
protobuf==5.26.1
pyyaml==6.0.1
# Optional: uncomment if you parse emojis/hashtags specially
# emoji==2.12.1

# Notes:
# - If you hit build issues on Windows for tokenizers, ensure Rust toolchain is present
#   or rely on prebuilt wheels (these versions have wheels).
# - For reproducibility, keep these pins aligned with your paper's tag.
